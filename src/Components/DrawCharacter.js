import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

const DrawCharacter = () => {
  return (
    <SafeAreaView>
      <Text>Draw Character Component....</Text>
    </SafeAreaView>
  );
};

export default DrawCharacter;
